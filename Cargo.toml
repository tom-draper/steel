[package]
name = "steel"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.6"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.3", features = ["fs"] }
serde = { version = "1.0", features = ["derive"] }
